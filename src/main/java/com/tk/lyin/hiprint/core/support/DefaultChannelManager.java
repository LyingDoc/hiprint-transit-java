package com.tk.lyin.hiprint.core.support;

import org.springframework.stereotype.Component;
import com.tk.lyin.hiprint.core.ChannelManager;

@Component
public class DefaultChannelManager implements ChannelManager {
}
